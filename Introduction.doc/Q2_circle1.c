//This program is used to calculate the area and circumference of a circle using structures.
#include<stdio.h>
struct circle
{
	int radi;
};
void main()
{
	struct circle circumference,area;
	int circumference;
	printf("Enter the the radius of the circle : ");
	 circumference = 22/7.0 * radi * 2;
	 area = 22/7.0 * radi * radi;
	scanf("%d%d",&cicumference,&area);
	printf("This is the circumference : %d",cirumfernce);
	printf("This is the area : %d",area);
	
}
